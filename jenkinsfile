pipeline {
    agent any
    environment {     
        DOCKERHUB_CREDENTIALS= credentials('dhub')     
    }    
    stages {
        stage('Git Checkout') {
            steps {

            }
        }

        stage('App Build') {
            steps {

            }
        }

        stage('Tests') {
            steps {

            }
        }

        stage('Node.js App Build') {
            steps {
                script {
                }
            }
